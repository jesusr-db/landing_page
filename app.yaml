command:
  - uvicorn
  - backend.main:app
  - --host=0.0.0.0
  - --port=8000
env:
  - name: DATABRICKS_HOST
    valueFrom:
      fieldRef:
        fieldPath: spec.databricks_host
user_api_scopes:
  - iam.current-user:read
  - iam.access-control:read
